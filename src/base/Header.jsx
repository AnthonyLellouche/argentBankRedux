import React from 'react';
import "../styles/base/_header.scss"

const Header = () => {
    return (
        <div className='header'>
            HEADER
        </div>
    );
};

export default Header;